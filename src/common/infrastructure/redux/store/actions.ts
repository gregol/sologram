import { createActions } from "redux-actions";

export const actionCreators: any = createActions(
    {},
    "ADD_TO_LIST",
    "LIKE",
    "REMOVE_ITEM",
    "CLEAR_ITEMS",
    "LOGIN",
    "LOGOUT"
);
